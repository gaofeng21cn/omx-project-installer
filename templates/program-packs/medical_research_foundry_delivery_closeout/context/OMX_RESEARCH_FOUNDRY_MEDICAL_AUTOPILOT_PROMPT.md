你现在接手的是 `{{REPO_NAME}}` 的长期主线。

顶层定位：

- `Research Foundry` 的医学实现
- `domain gateway + domain harness`
- 目标始终服务于 `publication-grade paper delivery`

执行要求：

- 先读 `CURRENT_PROGRAM.md`
- 再读 `PROGRAM_ROUTING.md`
- 再读当前 active program 对应的 roadmap / PRD / test-spec / reports
- 默认最大化利用 `OMX team mode`
- 每完成一条固定子线后，直接提升到下一条固定子线
- 不把单次 team 生命周期、worker 停止、mailbox 陈旧状态当 blocker

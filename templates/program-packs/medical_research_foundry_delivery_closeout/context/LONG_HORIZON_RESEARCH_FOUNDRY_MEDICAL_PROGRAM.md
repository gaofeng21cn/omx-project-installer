# Long Horizon Program - {{PACK_MAINLINE_ID}}

## Positioning

这个 pack 把当前项目定位为：

- `Research Foundry` 的医学实现
- 对外稳定的 `domain gateway`
- 对内清晰的 `domain harness`
- 业务北极星始终是 `publication-grade paper delivery`

## Long-Horizon Order

1. 冻结当前唯一 active mainline
2. 收紧 harness authority convergence
3. 收紧 delivery / publication plane convergence
4. 完成 real-study relaunch and verify
5. 只有全部收口后，才允许进入 monorepo scaffold gate

## Working Rule

- 这份文档定义的是长期 program 方向
- 真正的当前 phase、active subline、blocking state 仍由 `CURRENT_PROGRAM.md` 与 reports 冻结
